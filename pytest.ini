[pytest]
testpaths = test
python_files = test_*.py
timeout = 600
timeout_method = thread
markers =
    integration: tests that call live external financial-market services
    slow: long-running integration tests against live external services
